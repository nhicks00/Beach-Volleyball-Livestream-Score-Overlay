[pytest]
testpaths = tests
python_files = test_*.py
addopts = --strict-markers -m "not slow"
markers =
    slow: integration or network-dependent scraper tests
norecursedirs = archived-versions node_modules venv scraper_venv
